extends baseEmail

block content 
    p Hi #{firstName}, 
    p Forgot Password? Submit the path request to with your new password and passwordConfirm to : #{url}.
    p (this is not implimented yet !!!)
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
        tbody
            tr
                td(align='left')
                    table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                        tbody
                            tr
                                td
                                    a(href=`${url}`, target='_blank') Reset Your Password 
    p if you didn't forgot then go and login again 